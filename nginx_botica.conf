server {
    listen 80;
    server_name botica.virgendehuata.com www.botica.virgendehuata.com;

    # Redirigir HTTP a HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name botica.virgendehuata.com www.botica.virgendehuata.com;

    # Certificados SSL (usar Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/botica.virgendehuata.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/botica.virgendehuata.com/privkey.pem;

    # Configuración SSL
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    client_max_body_size 75M;

    location /static/ {
        alias /home/botica/proyecto_botica/staticfiles/;
    }

    location /media/ {
        alias /home/botica/proyecto_botica/media/;
    }

    location / {
        proxy_pass http://unix:/run/gunicorn.sock;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }

    # Logs
    access_log /var/log/nginx/botica_access.log;
    error_log /var/log/nginx/botica_error.log;
}
